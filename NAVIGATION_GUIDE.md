# 右侧导航菜单系统使用指南

## 概述

本项目已实现了一个类似 Ant Design 的右侧导航菜单系统，包含以下特性：

- 🎨 **美观的 UI 设计** - 类似 Ant Design 的视觉风格
- 📱 **响应式布局** - 支持移动端和桌面端
- 🔄 **可折叠侧边栏** - 支持展开/折叠功能
- 🎯 **智能导航** - 点击菜单项自动切换内容
- 🏷️ **版本徽章** - 显示组件版本信息
- 📂 **层级菜单** - 支持多级菜单结构

## 系统架构

### 组件结构

```
src/components/layout/
├── MainLayout.tsx          # 主布局组件
├── Sidebar.tsx             # 右侧侧边栏组件
├── NavigationMenu.tsx      # 导航菜单组件
├── MainLayout.css          # 主布局样式
├── Sidebar.css             # 侧边栏样式
├── NavigationMenu.css      # 导航菜单样式
└── index.ts                # 组件入口文件
```

### 核心组件

#### 1. MainLayout 主布局组件

- 管理整体布局结构
- 控制侧边栏的展开/折叠状态
- 传递导航相关的 props

#### 2. Sidebar 侧边栏组件

- 右侧固定定位的导航面板
- 包含 Logo、折叠按钮、导航菜单和版本信息
- 支持响应式设计

#### 3. NavigationMenu 导航菜单组件

- 渲染层级菜单结构
- 支持菜单项的展开/折叠
- 处理菜单项的选中状态

## 使用方法

### 基本使用

```typescript
import { MainLayout } from "@components/layout";
import { useNavigation } from "@hooks/useNavigation";
import ContentRenderer from "@components/ContentRenderer";

function App() {
  const { selectedKey, handleMenuSelect } = useNavigation();

  return (
    <MainLayout selectedKey={selectedKey} onMenuSelect={handleMenuSelect}>
      <ContentRenderer selectedKey={selectedKey} />
    </MainLayout>
  );
}
```

### 自定义菜单数据

在 `NavigationMenu.tsx` 中修改 `menuData` 数组：

```typescript
const menuData: MenuItem[] = [
  {
    key: "overview",
    title: "组件总览",
    icon: "📋",
  },
  {
    key: "general",
    title: "通用",
    icon: "🔧",
    children: [
      {
        key: "button",
        title: "Button 按钮",
        icon: "🔘",
      },
    ],
  },
];
```

### 添加新的内容页面

1. 在 `ContentRenderer.tsx` 中添加新的 case：

```typescript
case 'new-page':
  return <NewPageComponent />;
```

2. 在 `useNavigation.ts` 中更新 `getCurrentContent` 方法：

```typescript
const getCurrentContent = useCallback(() => {
  switch (selectedKey) {
    case "new-page":
      return "new-page";
    // ... 其他 case
  }
}, [selectedKey]);
```

## 样式定制

### 主题色彩

在 CSS 文件中修改以下变量：

```css
/* 主色调 */
--primary-color: #1890ff;
--primary-hover: #40a9ff;

/* 背景色 */
--bg-color: #fff;
--bg-hover: #f5f5f5;

/* 边框色 */
--border-color: #f0f0f0;
```

### 尺寸调整

```css
/* 侧边栏宽度 */
.sidebar {
  width: 280px; /* 展开时 */
}

.sidebar.collapsed {
  width: 80px; /* 折叠时 */
}

/* 主内容区域边距 */
.main-content {
  margin-right: 280px; /* 展开时 */
}

.main-content.collapsed {
  margin-right: 80px; /* 折叠时 */
}
```

## 功能特性

### 1. 菜单层级

- **一级菜单**：主要分类（通用、布局、导航等）
- **二级菜单**：具体组件（Button、Layout 等）
- **三级菜单**：组件变体（可选）

### 2. 交互效果

- **悬停效果**：鼠标悬停时背景色变化
- **选中状态**：当前选中项高亮显示
- **展开动画**：子菜单展开/折叠的平滑动画

### 3. 响应式设计

- **桌面端**：完整显示侧边栏
- **平板端**：可折叠的侧边栏
- **移动端**：隐藏侧边栏，可通过按钮显示

### 4. 版本徽章

- 显示组件的最新版本
- 绿色徽章表示新功能
- 自动隐藏折叠状态下的徽章

## 最佳实践

### 1. 菜单组织

- 按功能分类组织菜单项
- 使用清晰的图标和标题
- 保持层级结构简洁

### 2. 内容管理

- 每个菜单项对应一个独立的内容组件
- 使用 ContentRenderer 统一管理内容切换
- 保持组件间的解耦

### 3. 状态管理

- 使用 useNavigation Hook 管理导航状态
- 避免在组件中直接操作导航状态
- 保持状态的一致性

### 4. 性能优化

- 使用 React.memo 优化组件渲染
- 避免不必要的重新渲染
- 合理使用 useCallback 和 useMemo

## 扩展功能

### 1. 搜索功能

可以添加搜索框来快速定位菜单项：

```typescript
const [searchTerm, setSearchTerm] = useState("");
const filteredMenuData = menuData.filter((item) =>
  item.title.toLowerCase().includes(searchTerm.toLowerCase())
);
```

### 2. 面包屑导航

显示当前页面的层级路径：

```typescript
const breadcrumbItems = [
  { title: "首页", path: "/" },
  { title: "通用", path: "/general" },
  { title: "Button", path: "/general/button" },
];
```

### 3. 主题切换

支持明暗主题切换：

```typescript
const [theme, setTheme] = useState<"light" | "dark">("light");
```

### 4. 国际化

支持多语言菜单：

```typescript
const menuData = {
  zh: [...],
  en: [...]
};
```

## 注意事项

1. **路径别名**：确保 `@components` 路径别名配置正确
2. **样式冲突**：注意避免与现有样式冲突
3. **性能考虑**：大量菜单项时考虑虚拟滚动
4. **可访问性**：添加适当的 ARIA 标签和键盘导航
5. **浏览器兼容**：测试不同浏览器的兼容性

## 故障排除

### 常见问题

1. **菜单不显示**：检查 CSS 样式是否正确加载
2. **点击无响应**：确认事件处理函数正确绑定
3. **样式错乱**：检查 CSS 优先级和选择器
4. **构建失败**：确认所有依赖正确安装

### 调试技巧

1. 使用浏览器开发者工具检查元素
2. 查看控制台错误信息
3. 使用 React DevTools 调试组件状态
4. 检查网络请求和资源加载
